import React from "react";
import "./Home.css"; // Make sure this path is correct

const Home = () => {
  return (
    <div className="logo-container">
      {/* The logo is set as a background image via CSS */}
    </div>
  );
};

export default Home;
